<Window x:Class="PlantWidget.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подтверждение" Width="280" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    <Border Background="#EE2B3A28" CornerRadius="10" BorderBrush="#3A5A3A" BorderThickness="1">
        <StackPanel>
            <!-- Header -->
            <Grid Margin="14,10,8,4" MouseLeftButtonDown="Header_MouseLeftButtonDown" Background="Transparent">
                <TextBlock x:Name="TitleText" Text="Подтверждение" FontSize="14" FontWeight="Bold" Foreground="#E8F0E8" VerticalAlignment="Center"/>
                <Button Content="✕" Width="24" Height="24" HorizontalAlignment="Right"
                        Style="{StaticResource PlainIconButtonStyle}"
                        Click="No_Click"/>
            </Grid>

            <StackPanel Margin="16,4,16,16">
                <TextBlock x:Name="MessageText" Foreground="#C8D8C8" TextWrapping="Wrap" Margin="0,0,0,16"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Отмена" Style="{StaticResource DialogButtonStyle}" Margin="0,0,8,0" Click="No_Click"/>
                    <Button x:Name="ConfirmButtonText" Content="Удалить" Style="{StaticResource DialogButtonStyle}" Click="Yes_Click" IsDefault="True"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
